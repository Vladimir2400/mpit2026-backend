# Dating App Backend (MVP)

Backend для мобильного приложения знакомств, разработанный на Golang с чистой архитектурой для хакатона MPIT 2026.

## Особенности

- 🏗️ **Clean Architecture** - четкое разделение слоев (domain, usecase, repository, delivery)
- 🔐 **Безопасность** - JWT аутентификация, bcrypt хеширование, AES-256-GCM шифрование сообщений
- 🧠 **AI рекомендации** - Rule-based алгоритм с поддержкой MBTI, интересов, геолокации
- 💬 **Чат** - Зашифрованный обмен сообщениями
- 💳 **Подписки** - Мок-имплементация (мужчины платят, женщины бесплатно)
- 📱 **REST API** - 30+ endpoints для всех операций
- 🇷🇺 **Compliance** - Соблюдение ФЗ-152 и ФЗ-69

## Технологический стек

- **Язык**: Go 1.21+
- **Web Framework**: Gin
- **БД**: PostgreSQL 15+ (GORM)
- **Кэш**: Redis 7+
- **Миграции**: golang-migrate
- **JWT**: golang-jwt/jwt/v5
- **Валидация**: go-playground/validator

## Быстрый старт

### Предварительные требования

- Go 1.21+
- Docker & Docker Compose
- Make (опционально)

### Установка

1. Клонируйте репозиторий:
```bash
cd mpit2026-backend
```

2. Скопируйте и настройте конфигурацию:
```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

3. Запустите инфраструктуру и миграции:
```bash
make setup
```

Или вручную:
```bash
# Запустить PostgreSQL и Redis
docker-compose up -d

# Установить golang-migrate
go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest

# Выполнить миграции
migrate -path migrations -database "postgresql://dating_user:dating_pass@localhost:5432/dating_db?sslmode=disable" up
```

4. Запустите приложение:
```bash
make run
```

или

```bash
go run cmd/api/main.go
```

Сервер запустится на `http://localhost:8080`

## Структура проекта

```
mpit2026-backend/
├── cmd/
│   └── api/                 # Точка входа
├── internal/
│   ├── domain/             # Бизнес-сущности
│   ├── usecase/            # Бизнес-логика
│   ├── repository/         # Доступ к данным
│   ├── delivery/http/      # HTTP handlers & middleware
│   └── config/             # Конфигурация
├── pkg/                    # Утилиты (crypto, jwt, validator)
├── migrations/             # SQL миграции
├── docker-compose.yml
├── Makefile
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация пользователя
- `POST /api/v1/auth/login` - Вход
- `POST /api/v1/auth/refresh` - Обновление токена
- `POST /api/v1/auth/logout` - Выход

### Профиль
- `GET /api/v1/profile` - Получить профиль
- `PUT /api/v1/profile` - Обновить профиль
- `POST /api/v1/profile/photos` - Загрузить фото
- `DELETE /api/v1/profile/photos/:id` - Удалить фото

### Рекомендации
- `GET /api/v1/recommendations` - Лента рекомендаций

### Свайпы
- `POST /api/v1/swipes` - Свайпнуть (left/right/super)
- `GET /api/v1/swipes/history` - История свайпов

### Матчи
- `GET /api/v1/matches` - Все матчи
- `GET /api/v1/matches/:id` - Детали матча
- `DELETE /api/v1/matches/:id` - Размэтчить

### Чат
- `GET /api/v1/conversations` - Список чатов
- `GET /api/v1/conversations/:id/messages` - Сообщения
- `POST /api/v1/conversations/:id/messages` - Отправить сообщение

### Подписки (МОК)
- `GET /api/v1/subscriptions/plans` - Тарифы
- `POST /api/v1/subscriptions` - Создать подписку
- `GET /api/v1/subscriptions/current` - Текущая подписка

### Служебные
- `GET /health` - Health check

Полную документацию API см. в `/docs` (будет добавлено позже)

## База данных

### Схема

11 основных таблиц:
1. `users` - Пользователи (18+, email/phone, пароль)
2. `profiles` - Профили (имя, био, MBTI, интересы, геолокация, предпочтения)
3. `photos` - Фотографии профилей
4. `verification_data` - KYC верификация (мок для MVP)
5. `user_consents` - Согласия на обработку данных (ФЗ-152)
6. `swipes` - История свайпов
7. `matches` - Совпадения (взаимные лайки)
8. `conversations` - Беседы между матчами
9. `messages` - Зашифрованные сообщения
10. `subscriptions` - Подписки (мок)
11. `recommendation_logs` - Логи AI рекомендаций

### Миграции

```bash
# Применить миграции
make migrate-up

# Откатить последнюю миграцию
make migrate-down

# Создать новую миграцию
make migrate-create
```

## Алгоритм рекомендаций

Rule-based алгоритм с scoring:
- **MBTI совместимость** (30%) - Анализ психотипов
- **Общие интересы** (40%) - Пересечение интересов
- **Расстояние** (20%) - Геолокация
- **Активность** (10%) - Последняя активность

**Cold Start**: Для новых пользователей - показ по близости и популярности

**Балансировка**: 60% top + 30% mid + 10% random (избегаем popularity bias)

**Explainability**: Логирование причин рекомендаций

## Безопасность

- ✅ JWT токены (Access: 15 мин, Refresh: 30 дней)
- ✅ bcrypt для паролей (cost 12)
- ✅ AES-256-GCM для сообщений
- ✅ Валидация входных данных
- ✅ CORS middleware
- ✅ Проверка возраста 18+ на уровне БД

## Соответствие законодательству РФ

- **ФЗ-152**: Согласия на обработку персональных данных, soft delete, хранение на серверах в РФ
- **ФЗ-69**: Отдельное согласие на автопродление подписок
- **18+**: Constraint на уровне БД

## Разработка

### Запуск для разработки

```bash
make dev
```

### Тестирование

```bash
make test
```

### Сборка

```bash
make build
```

Бинарный файл будет создан в `bin/api`

## Make команды

```bash
make help          # Показать все доступные команды
make run           # Запустить приложение
make build         # Собрать приложение
make test          # Запустить тесты
make docker-up     # Запустить Docker контейнеры
make docker-down   # Остановить Docker контейнеры
make migrate-up    # Применить миграции
make migrate-down  # Откатить миграции
make seed          # Заполнить БД тестовыми данными
make clean         # Очистить артефакты сборки
make setup         # Полная настройка (docker + migrations)
make dev           # Режим разработки
```

## Конфигурация

Все настройки в `.env`:

```env
# Server
SERVER_HOST=localhost
SERVER_PORT=8080
ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=dating_user
DB_PASSWORD=dating_pass
DB_NAME=dating_db
DB_SSL_MODE=disable

# JWT
JWT_ACCESS_SECRET=your-secret-minimum-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-minimum-32-chars

# Encryption
AES_ENCRYPTION_KEY=32-byte-key-for-aes-256-gcm

# Storage
STORAGE_TYPE=local
STORAGE_PATH=./uploads
```

## MVP Scope

Базовый MVP для хакатона (3-5 дней):

✅ Core user flow: регистрация → профиль → свайпы → матчи → чат
✅ Rule-based AI рекомендации (расширяемо до ML)
✅ Базовый текстовый чат (REST API)
✅ Мок-подписки и KYC
✅ Шифрование и безопасность
✅ Соблюдение российских законов

## Потенциальные расширения

- 🚀 WebSocket для real-time чата
- 🧠 ML рекомендации (Python сервис + gRPC)
- 👤 Реальная KYC (face recognition API)
- 💰 Интеграция ЮKassa
- 📱 Push уведомления (FCM)
- 🛡️ Admin панель
- 📊 Аналитика и метрики
- 🔍 PostGIS для геопоиска
- ⚡ Rate limiting

## Команда

Разработано для хакатона MPIT 2026

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.

---

**🚀 Готово к демо!**
